# Development settings (no secrets in here — safe to commit)
# Chatbot server runs locally on port 4000 during development
REACT_APP_CHATBOT_URL=http://localhost:4000
# CSP disabled in dev so webpack hot-reload works
REACT_APP_CSP=
# Don't auto-open a browser tab on npm start
BROWSER=none
